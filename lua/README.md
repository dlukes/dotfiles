Keep this directory at least so that <https://github.com/folke/neodev.nvim> treats this
repo as a Lua plugin for Neovim, and properly configures sumneko's lua-language-server
when editing `init.lua`.
